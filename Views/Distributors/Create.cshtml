@model MoviesDBManager.Models.Distributor

@{
    ViewBag.Title = "Create";
}

@{ Html.RenderPartial("DistributorForm"); }

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script>
        $(function () {
            initTimeout(300);
        })
    </script>
}